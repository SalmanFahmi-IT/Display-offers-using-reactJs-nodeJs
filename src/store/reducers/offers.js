/**
 * OffersReducer
 */
const initialState = {

};
function offersReducer(state = initialState, action) {
    switch (action.type) {
      default:
        return state
    }
}

export default offersReducer;