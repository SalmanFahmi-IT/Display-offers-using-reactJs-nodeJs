/**
 * UserOffersReducer
 */
const initialState = {

};
function userOffersReducer(state = initialState, action) {
    switch (action.type) {
      default:
        return state
    }
}

export default userOffersReducer;